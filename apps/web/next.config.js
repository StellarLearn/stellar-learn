/**
 * StellarLearn Web — Next.js Configuration
 *
 * This config is intentionally minimal. Environment-specific settings
 * (Stellar network, Soroban RPC URL, etc.) are managed via .env variables.
 *
 * @see https://nextjs.org/docs/app/api-reference/next-config
 */

/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
